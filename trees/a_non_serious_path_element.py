class ANonSeriousPathElement:
    def __init__(
        self,
        feature_index,
        zero_fraction,
        one_fraction,
        path_weight,
    ):
        self.feature_index = feature_index
        self.zero_fraction = zero_fraction
        self.one_fraction = one_fraction
        self.path_weight = path_weight
